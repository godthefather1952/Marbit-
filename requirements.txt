# Core - everything needs these.
aiohttp>=3.9
websockets>=13.0
cryptography>=41.0       # Ed25519 (Polymarket US) and RSA-PSS (Kalshi) signing

# Performance (optional, but the bot tells you at startup if they're missing)
orjson>=3.9              # ~2.7x faster tick parsing than stdlib json

# Polymarket global CLOB ONLY. Not needed for Kalshi or the shared model -
# it pulls in the whole eth-account/web3 stack. Install with:
#     pip install -r requirements-polymarket.txt
